# Queue (memory for local, sqs for Lambda)
QUEUE_BACKEND=memory
# SQS_QUEUE_URL=          # required when QUEUE_BACKEND=sqs
# AWS_REGION=eu-west-1    # required when QUEUE_BACKEND=sqs

# Stripe webhook signing (from `stripe listen` or Dashboard)
# STRIPE_WEBHOOK_SECRET=whsec_xxx
